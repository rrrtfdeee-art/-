# 🗺️ خريطة ومعمارية منظومة Novelskyworld (NSW) الشاملة للذكاء الاصطناعي
> **تنبيه للنماذج الذكية والوكلاء (AI Agents & LLMs):** هذا الملف هو المرجع المعماري والتشغيلي الشامل لمنظومة ترجمة ونشر فصول الروايات. اقرأ هذا الملف دائماً قبل البدء بأي تعديل لتفهم مسارات البيانات وقواعدها دون الحاجة للشرح المتكرر من المستخدم.

---

## 1. الهوية والهدف العام (System Purpose)
منظومة آلية متكاملة لترجمة، تدقيق، جدولة، ونشر فصول الروايات الصينية على منصة **Blogger (مدونة عالم سماء الروايات)** وتوثيقها بالتزامن في قواعد بيانات **Google Sheets** مع ربط أزرار التنقل الذكية وإصلاح الفجوات المفقودة وصقل المتون عبر **Claude Opus** و **Gemini AI**.

---

## 2. خريطة الملفات والمسؤوليات (File & Component Map)

### بيئة بايثون الرئيسية (`C:\Users\Dell\.gemini\antigravity-ide\scratch\smart-media-bot`):
| الملف | الوظيفة والمسؤولية |
| :--- | :--- |
| `start.py` | مشغل النظام الموحد: يطلق بوت تيليجرام كخدمة خلفية (Daemon) + واجهة Streamlit مع إشعار الإقلاع التلقائي. |
| `telegram_bot.py` | واجهة التحكم والمراقبة عبر تيليجرام للمشرف مع أوامر: `/nsw_status`, `/nsw_gaps`, `/nsw_heal`, `/nsw_stage`, `/nsw_fix`, `/nsw_publish`, `/nsw_stop`. |
| `nsw_healer_engine.py` | المحرك المركزي للمنظومة: فحص الفجوات المفردة والمتعددة، سحب الفصول من المواقع الصينية، الترجمة الثلاثية الملكية، النشر، وربط أزرار التنقل ثنائياً. |
| `opus_staging_pipeline.py` | محرك صقل الفصول عبر **Claude Opus**: تجريد المتن من وسوم HTML، تجهيز دفعة الـ 20 فصلاً، إعادة التغليف الملكي التلقائي، والتحديث المزدوج (شيت أو بلوجر PATCH). |
| `gemini_analyzer.py` | وسيط الذكاء الاصطناعي: إدارة مجمع وسائط Multi-GAS Pool، وسلسلة النماذج النشطة (`gemini-3.5-flash-lite` ➔ `gemini-3.1-flash-lite` ➔ `gemini-3.8-flash`)، وحظر النماذج ذات الحصة الصفرية. |
| `scraper_engine.py` | محرك التصفح الخفي والسحب المتقدم مع تجاوز الحماية واستخراج المتون من المواقع المصدرية. |
| `database.py` | إدارة قاعدة البيانات المحلية SQLite (`novel_scraper.db`) لحفظ الإعدادات والمصطلحات وحالة المهام. |

### كود Google Apps Script السحابي (`c:\Novelskyworld\نظام ترجمة ونشر الفصول\كود النشر.txt` و `test_clean.js`):
| المكون السحابي | الوظيفة والمسؤولية |
| :--- | :--- |
| `recordPublishedChapterInAllDatabases` | التحديث المتزامن الصارم لقاعدتي البيانات (الشيت العام `1IFT` + شيت المنظومة `1HDj` الصفحة الخامسة / Published Posts). |
| `syncChapterBidirectionalNavigation` | الربط التلقائي ثنائي الاتجاه لأزرار التنقل: يربط زر "التالي" في الفصل السابق ($N-1$) وزر "السابق" في الفصل اللاحق ($N+1$). |
| `repairAllChapterNavigationLinks` | صيانة شاملة لكافة أزرار التنقل لجميع فصول الرواية دفعة واحدة بالتسلسل دون قفزات. |
| `runFullBloggerAndSystemAudit` | الفحص الشامل لحالة بلوجر (الحية، المجدولة، والمسودات)، واعتبار المسودات الخالية (0 حرف) فجوات مفقودة في `missingGaps`. |
| `createPost` (معالجة `doPost`) | النشر المباشر أو الجدولة، مع دعم ترقية المسودات القائمة وتعديلها بدلاً من تكرار التدوينات. |
| `updatePostContent` (معالجة `doPost`) | تعديل متن التدوينة الحية أو المجدولة عبر Blogger PATCH مع الحفاظ الصارم على الرابط والتصنيفات وتاريخ النشر. |

---

## 3. قواعد البيانات وجداول Google Sheets الرسمية

| المعرف (Spreadsheet ID) | اسم الجدول / الاستخدام | الأوراق والأعمدة المستهدفة |
| :--- | :--- | :--- |
| **`1IFT9mKRFByiPhph-ZaSdUT7c6IPUWpLg6Gj2xa9g5mY`** | **شيت المنشورات الحية العام** (قاعدة بيانات المدونة 1) | ورقة 1 (8 أعمدة): `ChapterNum`, `Title`, `Labels`, `PublishDate`, `PostID`, `PostURL`, `PublishedAt`, `Novelname`. |
| **`1HDjYu6EypdiNfoawJ2s7nQcRJiGsfJ5bNefcEy0QhFE`** | **شيت طابور النشر وقاعدة البيانات** (قاعدة بيانات المدونة 2) | ورقة 1: `Queue` (الطابور المجدول)<br>ورقة 2: `Failed Posts`<br>**الصفحة الخامسة / `Published Posts`** (الأرشيف التزامني). |
| **`1v1V4_rQukDs3oCe8Z4Izvni3uCx91iKmSVNOm4A3mH0`** | **شيت الترجمة والتدقيق** | ورقة `TranslateQueue` (المتون الخام والمترجمة)<br>ورقة `ReviewQueue` (فصول بانتظار التحكيم). |
| **`1oqKRLyqWdkdUWW5UtvorEteFk3jJXdeUzQGDv-XJ_aE`** | **شيت القاموس المعتمد** | المصطلحات الرسمية، أسماء الشخصيات والتقنيات لكل رواية لمنع التشويه. |
| **`1s-yf1gRHagPIeikEC9_aVIAst7oDaiwoNzLH-hd0Q24`** | **شيت فهارس الروايات** | روابط الفهارس الأصلية وإعدادات محددات الـ CSS. |

---

## 4. إعدادات Blogger API السحابية
* **معرف المدونة (Blog ID):** `5954999330508310240` (عالم سماء الروايات).
* **رابط Apps Script WebApp:**
  `https://script.google.com/macros/s/AKfycbxqLaqJru1ag-am7G9Mrwy5Nb7HliZlK5vbIEQD9MeV3wOOquNUvz4d7vWEwZxkBI6zIw/exec`

---

## 5. دليل نماذج الذكاء الاصطناعي والحصص (`Gemini_API_Rate_Limits.xlsx`)

| اسم النموذج | RPM | TPM | RPD (الحصة اليومية) | الدور المخصص في المنظومة |
| :--- | :---: | :---: | :---: | :--- |
| **`Gemini 3.5 Flash Lite`** | 15 | 250K | **500** | **النموذج الأساسي الأول** للترجمة وسحب الفصول وملء الفجوات. |
| **`Gemini 3.1 Flash Lite`** | 15 | 250K | **500** | **الاحتياطي التلقائي المباشر** عند استنفاد حصة 3.5 لمنع توقف السلسلة. |
| **`Gemini 3.8 Flash`** | 5 | 250K | **20** | التحكيم والتقييم اللغوي عالي الدقة والمقارنة المعيارية. |
| **`Claude Opus`** | - | - | حصة الجلسة | **الصقل الأدبي الأعلى** للمتون الصافية بعد تجريدها من الـ HTML. |
| **النماذج المحظورة (0.0 Quota)** | 0 | 0 | **0** | `Gemini 2.5 Pro`, `Gemini 3.1 Pro`, `Gemini 2 Flash` (ممنوع استدعاؤها نهائياً). |

---

## 6. معمارية دورة صقل Claude Opus (Token-Saving Architecture)

```
       [الفصول المنتظرة للاعتماد في الشيت أو المجدولة في بلوجر (حتى 20)]
                                  │
                                  ▼
      [تجريد النص برمجياً: strip_html_to_clean_story (توفير 60%+ توكنز)]
                                  │
                                  ▼
      [صقل أدبي واختيار بلاغي بحت عبر Claude Opus (بدون أي كود HTML)]
                                  │
                                  ▼
      [إعادة التغليف الملكي التلقائي: wrap_clean_story_to_royal_html]
                                  │
                 ┌────────────────┴────────────────┐
                 ▼                                 ▼
   [إن كان الفصل في الشيت]          [إن كان الفصل في بلوجر مجدولاً/حياً]
                 │                                 │
                 ▼                                 ▼
    تحديث عمود النص بالشيت             إرسال PATCH: updatePostContent
     لحين موعد نشره المجدول            (الحفاظ التام على الرابط والتاريخ والتصنيفات)
```

---

## 7. القواعد والوصايا الصارمة للذكاء الاصطناعي (Strict Operational Commandments)

1. **حظر تكرار التدوينات (No Duplicate Posts):**
   إذا كان الفصل موجوداً في بلوجر بالفعل كمسودة فارغة أو منشور ناقص وله معرف `postId`، **يحظر إنشاء تدوينة جديدة**؛ بل يتم تعديل نفس المسودة القائمة وترقيتها وجدولتها عبر `publishBloggerDraft`.
2. **قدسية الروابط والتواريخ (Preserve Slugs & Dates):**
   عند تحديث أو استصلاح أي فصل، يمنع منعاً باتاً تغيير رابط التدوينة أو تاريخ نشرها أو تصنيفاتها؛ التعديل يستهدف متن القصة فقط.
3. **التحديث المزدوج الإلزامي (Dual-Database Sync):**
   أي نشر أو جدولة لفصل (جديد أو فجوة) يجب أن يكتب فوراً في:
   - شيت `1IFT9mKRFByiPhph-ZaSdUT7c6IPUWpLg6Gj2xa9g5mY`
   - شيت `1HDjYu6EypdiNfoawJ2s7nQcRJiGsfJ5bNefcEy0QhFE` (الصفحة الخامسة / Published Posts).
4. **الربط التسلسلي التلقائي (Bidirectional Nav):**
   عند نشر الفصل $N$، يجب ربط زر "التالي" في الفصل $N-1$، وربط زر "السابق" في الفصل $N+1$، لضمان استمرارية القراءة بدون شاشات معلقة.
5. **اقتصاد التوكنز لأوبس (Zero-HTML Output for Opus):**
   لا يُطلب من Claude Opus كتابة أكواد HTML أبداً؛ التجريد والتغليف وظيفة برمجية بايثونية بحتة.
6. **سلامة الأسماء الصينية:**
   - `老马` = العم ما / العجوز ما (وليس "ماء").
   - `木头琴` = مو تشين (وليس "كمان الخشب").
   - الرجوع الدائم لشيت القاموس `1oqK...` لتوحيد مسميات الأبطال والتقنيات.
